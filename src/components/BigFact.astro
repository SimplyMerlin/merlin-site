---
export interface Props {
  header: string;
  description: string;
}

const { header, description } = Astro.props;
---

<div class="flex flex-col gap-y-2">
  <span class="text-6xl font-extrabold">{header}</span>
  <span class="text-3xl font-semibold">{description}</span>
</div>
